<Window x:Class="ImageTransfer.Wpf.Views.RootView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vm="clr-namespace:ImageTransfer.Wpf"
        xmlns:views="clr-namespace:ImageTransfer.Wpf.Views"
        Title="图片大小转换工具"
        Width="640"
        MinWidth="640"
        SizeToContent="Height"
        ResizeMode="CanMinimize"
        Background="{DynamicResource RegionBrush}">
    <Window.DataContext>
        <vm:SingleTransfer />
    </Window.DataContext>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:SingleTransferView Grid.Row="0" />

        <ProgressBar Grid.Row="1"
                     Height="20"
                     Margin="0,12,0,0"
                     Minimum="0"
                     Maximum="100"
                     Value="{Binding Progress}" />
    </Grid>
</Window>